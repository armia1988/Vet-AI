name: One-time V19 auth lifecycle

on:
  push:
    paths:
      - '.github/workflows/one-time-v19-auth-lifecycle.yml'

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Apply V19 auth lifecycle patch
        run: python3 tools/patch_v19_auth_lifecycle.py
      - name: Get dependencies
        run: flutter pub get
      - name: Verify static analysis
        run: flutter analyze --no-fatal-infos
      - name: Commit verified V19 auth lifecycle
        run: |
          git config user.name 'Vet AI Build Bot'
          git config user.email 'actions@github.com'
          git add lib/v5_app.dart lib/services/vet_backend.dart
          if ! git diff --cached --quiet; then
            git commit -m 'Add V19 confirmation resend flow'
            git push
          fi
