name: One-time V6 UI patch

on:
  push:
    paths:
      - '.github/workflows/one-time-v6-ui-patch.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply UI patch
        run: python3 tools/patch_v6_ui.py
      - name: Commit UI patch
        run: |
          git config user.name 'Vet AI Build Bot'
          git config user.email 'actions@github.com'
          git add lib/v5_app.dart
          if ! git diff --cached --quiet; then
            git commit -m 'Add automatic manual languages and colored navigation'
            git push
          fi
